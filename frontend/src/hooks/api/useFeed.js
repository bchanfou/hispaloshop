/**
 * @deprecated Import feed queries from `features/feed/queries` instead.
 * This module remains as a compatibility path during the phase 1 migration.
 */

export * from '../../features/feed/queries';
